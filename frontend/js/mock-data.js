/**
 * Phase 4 — this file is intentionally empty.
 *
 * It previously held preview fixtures for dashboard surfaces with no backing
 * endpoint (Automations, Integrations, Analytics "trends", the AI persona
 * panel). Those surfaces were removed in the Phase 4 redesign: the dashboard
 * now shows ONLY real backend data, with a professional empty state when
 * there is none. `window.MOCK` is kept as an empty object so the
 * `<script src="js/mock-data.js">` tag in index.html stays valid without a
 * markup change.
 */
const MOCK = {};
